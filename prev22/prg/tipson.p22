typ a = bool
#var a : c # a ze deklariran
var d : a
var e : int
fun f(a : int) : a = {
    d = e;
    e = a;
    d = p() where {
        var x : int
        fun p() : int = 5
    };
    j.bcd
    where {
        var j : { bcd : int, ghi: bool }
    };
    e = f(e);
}
