typ rectype = {ena : int, one : {dva : int, two : {tri : int, three : {stiri : int, four : {pet : int, five : {sest: int, six : {sedem : int, seven : {osem : int}}}}}}}}


var globalna : int
var absolutniNaslov : int
var string : ^char

fun nicParam() : int
fun enParam(ena : int) : char
fun stiriParam(ena : int, dva : int, tri : char, stiri: int) : char

var num : int
var znak : char

fun main() : int = {
	stiriParam(num, num, znak, num);
	string = "bes\\"e\"da";

	num = num where {
		var spremenljivka : int
		var enaTabela : [10]char
		fun triParam(ena : int, dva : int, tri : int) : char = {
			globalna = 2;
			absolutniNaslov = 42;
			znak = znak where {
				fun dvaParam(ena : int, dva : char) : char
				var stevilo: int
			};
			znak;
		}
	};

	num;
}
