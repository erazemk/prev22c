var i : int
var c : char
var d : int

fun main() : int = {
    i = 0;

    while { i = i + 1; i < 5; } do {
        c = toChar(i);
        putc(c);
    };

    i;
}

fun toChar(j : int) : char = {
    d = j + ('0' : int);
    (d : char);
}

fun putc(c : char) : void
